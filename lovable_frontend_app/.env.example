# Lovable Frontend App - .env.example

# HTTP base URL to the backend API
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Optional explicit WebSocket base (if needed by app code)
# NEXT_PUBLIC_WS_URL=ws://localhost:3001
